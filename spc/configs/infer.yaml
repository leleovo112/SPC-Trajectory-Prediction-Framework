# configs/infer.yaml
# Inference-specific configuration.

train:
  device: cuda:0

llm:
  provider:    openai      # override with --provider flag at runtime
  model:       gpt-4-turbo
  temperature: 0.0
  max_tokens:  1024

logging:
  output_dir:    /home/carla_user/results
  llm_cache_dir: /home/carla_user/llm_cache
